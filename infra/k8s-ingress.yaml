apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ethical-trader-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: ethicaltrader.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ethical-trader-service
            port:
              number: 80
